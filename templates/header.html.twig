<div id="header">
    <table>
        <tr>
            <td align="center" width="10%"><img src="./img/logo.png" alt="logo eco-presto" float="left" width="200px"/></td>
            <td align="center" width="10%"><h1><a href="{{ path("app_home_page")}}">Eco-Presto</a></h1></td>
            <td align="right" width="70%"><p>{% if app.user %}
                Connecté en tant que {{ app.user.login }}
            {% else %}
                Non connecté
            {% endif %}</p></td>
        </tr>
    </table>
</div>
